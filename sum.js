const sum = (a, b) => a + b;

const product = (a, b) => a * b

const divide = (a, b) => a / b

const greeting = (name) => `Hello, ${name}!`

module.exports = { sum: sum, product: product, divide: divide, greeting: greeting };
